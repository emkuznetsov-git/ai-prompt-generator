# Prompt Generator v3.2

Генератор промптов для glamour/lingerie рекламных изображений (non-explicit) для нейросетей.

## Описание

Система генерирует текстовые промпты для нейросетей (движков генерации), чтобы получать фотореалистичные, журнального уровня изображения в жанре glamour / lingerie advertising, пригодные для соцсетей и журналов.

### Важные ограничения продукта

- **Только adult-контент**: модель 25+
- **Non-explicit**: без явной наготы и без сексуальных актов
- **Запрет look-alike**: не генерирует запросы вида "сделай как (имя реального человека)"
- **Запрет minors**: блокирует любые упоминания несовершеннолетних

## Структура проекта

Проект представляет собой простую HTML-страницу без зависимостей и сборки:

```
v-3-2/
├── index.html          # Главная страница
├── script.js           # Вся логика генерации (встроенные данные)
├── style.css           # Стили
├── README.md           # Документация
└── data/               # Исходные данные (JSON)
    ├── lexicons/       # Словари модулей
    │   ├── artDirection.json
    │   ├── beauty.json
    │   ├── camera.json
    │   ├── color.json
    │   ├── framing.json
    │   ├── goal.json
    │   ├── lighting.json
    │   ├── pose.json
    │   ├── scene.json
    │   ├── shoes.json
    │   ├── subject.json
    │   └── wardrobe.json
    └── presets.json    # Пресеты арт-дирекшна
```

Все данные (словари и пресеты) встроены непосредственно в `script.js` в виде констант `LEXICONS_DATA` и `PRESETS_DATA`, поэтому страница работает без сервера и без загрузки внешних файлов. 

**⚠️ Важно:** JSON файлы в папке `data/` служат исходными данными для встраивания в код, но **актуальная версия всегда находится в `script.js`**. При обновлении данных сначала обновляйте `script.js`, затем синхронизируйте JSON файлы для удобства редактирования.

**Статус синхронизации JSON файлов (по состоянию на последнюю проверку):**
- ✅ `presets.json` — синхронизирован (13 пресетов)
- ✅ `goal.json` — синхронизирован (11 элементов)
- ✅ `subject.json` — синхронизирован (10 элементов)
- ✅ `artDirection.json` — синхронизирован (13 элементов)
- ✅ `beauty.json` — синхронизирован (14 элементов)
- ✅ `wardrobe.json` — синхронизирован (19 элементов)
- ✅ `shoes.json` — синхронизирован (17 элементов)
- ⚠️ `scene.json`, `pose.json`, `lighting.json`, `camera.json`, `color.json` — требуют проверки синхронизации (большой объем данных, актуальная версия в `script.js`)

**Примечание:** Модули `scene`, `pose`, `lighting`, `camera`, `color` содержат большое количество элементов (20-30+ каждый). Для полной синхронизации этих модулей рекомендуется извлечь данные напрямую из `script.js` (константа `LEXICONS_DATA`).

## Запуск

Просто откройте `index.html` в браузере. Работает локально без сервера.

### Альтернативный запуск (опционально)

Если хотите использовать локальный сервер (для отладки):

```bash
# Python 3
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server

# Затем откройте http://localhost:8000
```

---

## Логика работы генератора

### Общая схема генерации

Генерация промпта происходит в несколько этапов:

1. **Разрешение выборов** (`resolveSelections`) — определение, какие элементы использовать для каждого модуля
2. **Сборка частей промпта** в строгом порядке (11 модулей)
3. **Применение boldness модификаторов** — вставка модификатора смелости
4. **Построение negative constraints** — сборка негативных ограничений
5. **Применение safety фильтров** — санитизация запрещенных паттернов
6. **Адаптация под движок** — применение адаптера для конкретного движка

### 1. Разрешение выборов (resolveSelections)

Система определяет, какие элементы из словарей использовать для каждого модуля.

**Приоритет выбора (от высшего к низшему):**

1. **Ручной выбор пользователя** (`manualSelections`) — самый высокий приоритет
2. **Пресет** (`preset.selections`) — применяется, если модуль не выбран вручную
3. **Рандомный выбор** — если `randomMode = 'full'` или `'guided'` и модуль не выбран

**Логика рандома:**

- Если выбран пресет, рандомные элементы фильтруются по тегам пресета (soft filtering)
- Используется weighted random (элементы с большим `weight` выбираются чаще)
- RNG seedable для воспроизводимости результатов
- Учитывается уровень boldness для взвешивания элементов (B2/B3 предпочитают "hot" элементы)

**Специальные случаи:**

- Если `enforceFullLength = true`, модуль `framing` всегда устанавливается автоматически
- Для Grok `enforceFullLength` всегда `true` (принудительно)

**Weighted random с учетом boldness:**

Система динамически изменяет веса элементов в зависимости от уровня boldness:

- **Subject**: B2/B3 увеличивают вес "hot/sexy/edgy" архетипов (×1.35), уменьшают "elegant" (×0.9)
- **Beauty**: B2/B3 увеличивают вес "hot" макияжа (×1.55), учитывается совместимость с прической субъекта
- **Wardrobe/Shoes**: B2/B3 значительно увеличивают вес "hot" элементов (×2.2), уменьшают "classic" (×0.7)
- **Scene**: B2/B3 предпочитают ночные/backstage/hotel/noir сцены (×1.25)

### 2. Порядок сборки промпта

Промпт собирается в **строгом порядке** (важно для качества результата):

```
1. FRAMING (если enforceFullLength=true)
   └─ "FULL-LENGTH LONG SHOT, head-to-toe full body in frame, 
       feet visible, shoes/boots visible, include floor, 
       camera pulled back, wide framing, 
       leave margin above head and below feet."

2. GOAL
   └─ "lingerie ad campaign" / "high-end glamour magazine editorial photo"

3. ART-DIRECTION (пресет)
   └─ "Playboy-inspired high-end glamour magazine editorial"

4. SUBJECT (всегда включает "adult woman (25+)")
   └─ "adult woman (25+), blonde bombshell vibe, confident direct gaze"

4b. HAIR LOCK (стабилизация цвета волос)
   └─ "Hair color: platinum blonde hair (keep hair color consistent)."
   └─ Negative: "No brunette hair, no black hair, no red hair."

5. BEAUTY
   └─ "glossy long brunette waves, smoky eyes, nude glossy lips..."

6. WARDROBE (объединяет lingerie + shoes)
   └─ "Wardrobe: black lace corset lingerie..., nude patent stilettos"

7. SCENE
   └─ "Scene: clean studio cyclorama with glossy floor..."

8. POSE/ACTION
   └─ "Pose: standing full body near bed, one hand holding..."

9. LIGHTING
   └─ "Lighting: softbox key light + gentle rim light..."

10. CAMERA/COMPOSITION
    └─ "Camera: 50mm editorial look, eye-level with slight low angle..."

11. COLOR/RETOUCH
    └─ "warm-neutral filmic color grading, natural skin texture..."
```

**Особенности сборки:**

- **Subject**: всегда начинается с "adult woman (25+)", даже если выбран архетип
- **Hair Lock**: автоматически добавляется на основе тегов выбранного subject для стабилизации цвета волос
- **Wardrobe**: объединяет lingerie и shoes в одну секцию "Wardrobe: ..."
- **Секции с префиксами**: Scene, Pose, Lighting, Camera начинаются с соответствующих префиксов

### 3. Применение Boldness

Boldness модификаторы вставляются после art-direction части.

**Уровни Boldness:**

- **B0**: "tasteful lingerie campaign, elegant pose, strategic coverage"
- **B1**: "provocative glamour editorial, teasing but classy"
- **B2**: "bold glamour, implied nudity but covered, tasteful framing"
- **B3**: "very bold glamour, on the edge, cinematic low-key, still non-explicit, no sexual act"

**Алгоритм вставки:**

1. Ищется часть промпта, содержащая "glamour" или "boudoir"
2. Модификатор вставляется после этой части через запятую
3. Если не найдено, модификатор добавляется в начало промпта

### 4. Построение Negative Constraints

Negative всегда включает следующие секции (в порядке добавления):

1. **Safety constraints:**
   ```
   No explicit nudity, no sexual act, no minors.
   ```

2. **Full-length guards** (если `enforceFullLength=true`):
   ```
   No close-up, no portrait crop, no half-body, no cropped head, 
   no cropped feet, no out-of-frame shoes/boots.
   ```

3. **Hair lock negative** (если применим):
   ```
   No brunette hair, no black hair, no red hair.
   ```

4. **Технические негативы:**
   ```
   No text, no watermark, no logos.
   No extra fingers/limbs, no warped hands, no distorted face, 
   no blur, no low-res, no plastic skin.
   ```

### 5. Safety Фильтры

Все промпты проходят через `SafetyFilter.filter()` перед возвратом.

**Блокируемые паттерны:**

1. **Look-alike запросы:**
   - Паттерн: `/\b(a-la|look like|looks like|similar to|resembling|in the style of)\s+[A-Z][a-z]+(?:\s+[A-Z][a-z]+)?\b/gi`
   - Действие: заменяется на "glamour archetype"
   - Пример: "make her look like Emma Watson" → "glamour archetype"

2. **Minors references:**
   - Паттерны: `teen(ager|age)?`, `school(girl|boy|kid|child)`, `young (girl|boy|kid|child|looking)`, `underage`, `minor`, `very young`
   - Действие: удаляется из текста

3. **Explicit content:**
   - Паттерны: `explicit`, `nude`, `naked`, `genital`, `sexual act`, `porn`, `xxx`
   - Действие: удаляется из текста

**Результат фильтрации:**

- `sanitized`: очищенный текст
- `blocked`: флаг, был ли заблокирован контент
- `warnings`: массив предупреждений для пользователя

### 6. Адаптеры движков

После генерации промпт проходит через адаптер для конкретного движка.

#### GrokAdapter

**Особенности:**

- Усиливает framing для full-length
- Дублирует crop-guards в negative для надежности
- Если промпт не начинается с "FULL-LENGTH", добавляет framing prefix

**Логика:**

```javascript
if (enforceFullLength) {
    // Добавить framing в начало, если его нет
    if (!promptText.startsWith('FULL-LENGTH')) {
        promptText = framingPrefix + ' ' + promptText;
    }
    // Усилить negative с crop-guards
    if (!negativeText.includes('no close-up')) {
        negativeText = cropGuards + ' ' + negativeText;
    }
}
```

#### GenericAdapter

Простая реализация без модификаций — возвращает результат как есть.

---

## Формат ввода-вывода параметров

### Входные параметры (State)

Объект `state`, передаваемый в `PromptGenerator.generate()`:

```typescript
{
    engine: string;              // 'grok' | 'generic'
    format: string;              // 'IG_4x5' | 'Story_9x16' | 'Print_2x3'
    preset?: string;              // ID пресета или undefined
    enforceFullLength: boolean;  // Принудительный full-length контроль
    boldness: string;             // 'B0' | 'B1' | 'B2' | 'B3'
    manualSelections: {           // Ручные выборы пользователя
        [module: string]: string;  // module -> itemId
    };
    randomMode: string;           // 'manual' | 'guided' | 'full'
}
```

**Вариации параметров:**

#### engine
- **`'grok'`**: Grok (X.AI) — всегда включает `enforceFullLength=true`
- **`'generic'`**: Универсальный формат для других движков

#### format
- **`'IG_4x5'`**: Instagram 4:5 (вертикальный формат для ленты)
- **`'Story_9x16'`**: Story/Reel 9:16 (вертикальный формат для сторис)
- **`'Print_2x3'`**: Print 2:3 (горизонтальный формат для печати)

#### preset
- ID пресета из `PRESETS_DATA` или `undefined` (без пресета)
- Пресет задает дефолтные выборы для нескольких модулей одновременно

#### enforceFullLength
- **`true`**: Включает full-length контроль (framing + crop-guards)
- **`false`**: Отключает (но для Grok всегда `true`)

#### boldness
- **`'B0'`**: Tasteful, elegant, strategic coverage
- **`'B1'`**: Provocative, teasing but classy (по умолчанию)
- **`'B2'`**: Bold, implied nudity but covered
- **`'B3'`**: Very bold, on the edge, cinematic low-key

#### randomMode
- **`'manual'`**: Только ручные выборы и пресет, рандом не применяется
- **`'guided'`**: Ручные выборы сохраняются, незаполненные модули заполняются рандомом
- **`'full'`**: Все модули выбираются рандомом (учитываются теги пресета)

#### manualSelections
Объект, где ключ — имя модуля, значение — ID элемента из словаря:

```javascript
{
    subject: 'blonde_bombshell',
    beauty: 'glossy_waves_smoky',
    wardrobe: 'black_lace_corset',
    // ... другие модули
}
```

**Доступные модули:**
- `framing` — кадрирование (full-length)
- `goal` — цель/назначение изображения
- `artDirection` — арт-дирекшн (стиль)
- `subject` — архетип модели
- `beauty` — прическа и макияж
- `wardrobe` — нижнее белье
- `shoes` — обувь/ботинки
- `scene` — сцена/локация
- `pose` — поза/действие
- `lighting` — освещение
- `camera` — камера/композиция
- `color` — цветокоррекция/ретушь

### Выходные данные (Result)

Объект, возвращаемый генератором:

```typescript
{
    prompt_text: string;          // Основной промпт
    negative_text: string;        // Негативные ограничения
    metadata: {                   // Метаданные генерации
        engine: string;
        format: string;
        preset?: string;
        selections: {             // Финальные выборы (после resolveSelections)
            [module: string]: string;
        };
        seed: number;             // Seed для воспроизводимости
        timestamp: string;        // ISO timestamp
        boldness: string;
        enforceFullLength: boolean;
    };
    warnings: string[];           // Предупреждения от safety фильтра
}
```

**Формат вывода:**

1. **Объединенный формат** (для копирования):
   ```
   [prompt_text]

   ---

   Negative:
   [negative_text]
   ```

2. **Раздельный формат**:
   - `prompt_text`: только основной промпт
   - `negative_text`: только негативные ограничения

3. **Metadata JSON** (для экспорта):
   ```json
   {
     "prompt_text": "...",
     "negative_text": "...",
     "metadata": {
       "engine": "grok",
       "format": "Story_9x16",
       "preset": "playboy_studio_glam",
       "selections": { ... },
       "seed": 123456,
       "timestamp": "2024-01-01T12:00:00.000Z",
       "boldness": "B1",
       "enforceFullLength": true
     },
     "warnings": []
   }
   ```

---

## Структура данных

### Lexicon Item (элемент словаря)

Каждый элемент словаря имеет следующую структуру:

```typescript
{
    id: string;                    // Уникальный идентификатор
    label: string;                 // Отображаемое название (для UI)
    text_en: string;               // Текст для вставки в промпт (английский)
    tags: string[];                // Теги для фильтрации по пресету и совместимости
    weight: number;                // Вес для weighted random (по умолчанию 1.0)
    constraints?: {                // Опционально: ограничения
        requires?: string[];       // Требуемые элементы (например, "include floor")
        excludes?: string[];       // Исключаемые элементы
    };
    engine_overrides?: {           // Опционально: переопределения для движков
        [engine: string]: string;   // engine -> альтернативный text_en
    };
}
```

**Примеры элементов:**

```json
{
  "id": "blonde_bombshell",
  "label": "Blonde bombshell",
  "text_en": "blonde bombshell vibe, confident direct gaze",
  "tags": ["blonde", "bombshell", "confident", "hair_blonde"],
  "weight": 1.0
}
```

```json
{
  "id": "patent_stilettos",
  "label": "Patent stilettos",
  "text_en": "nude patent stilettos",
  "tags": ["stilettos", "patent", "nude"],
  "weight": 1.0,
  "constraints": {
    "requires": ["include floor"]
  }
}
```

### Preset (пресет)

Пресет задает дефолтные выборы для нескольких модулей и теги для фильтрации:

```typescript
{
    id: string;                    // Уникальный идентификатор
    label: string;                 // Отображаемое название
    description: string;           // Описание пресета
    selections: {                   // Дефолтные выборы модулей
        [module: string]: string;  // module -> itemId
    };
    tags: string[];                // Теги для фильтрации рандомных выборов
}
```

**Пример пресета:**

```json
{
  "id": "playboy_studio_glam",
  "label": "Playboy-inspired Studio Glam",
  "description": "High-end studio glamour with clean cyclorama",
  "selections": {
    "artDirection": "playboy_studio_glam",
    "scene": "studio_cyclorama",
    "lighting": "softbox_key_rim",
    "camera": "50mm_editorial",
    "color": "warm_neutral_filmic"
  },
  "tags": ["studio", "high-key", "playboy", "glamour"]
}
```

---

## Описание модулей и их вариаций

### 1. Framing (Кадрирование)

**Назначение:** Контроль полного роста модели в кадре.

**Элементы:**
- `full_length_standard`: Стандартный full-length кадр

**Особенности:**
- Используется только при `enforceFullLength=true`
- Автоматически добавляется для Grok
- Текст: "FULL-LENGTH LONG SHOT, head-to-toe full body in frame, feet visible, shoes/boots visible, include floor, camera pulled back, wide framing, leave margin above head and below feet."

### 2. Goal (Цель/Назначение)

**Назначение:** Определяет цель и контекст использования изображения.

**Вариации (11 элементов):**
- `lingerie_ad_campaign`: Рекламная кампания нижнего белья (hero shot) — weight: 1.0
- `glamour_magazine_editorial`: Редакционная съемка для глянцевого журнала — weight: 1.0
- `luxury_lookbook_series`: Серия для лукбука — weight: 0.9
- `social_media_teaser_post`: Пост для соцсетей (лента) — weight: 0.95
- `social_story_reel_cover`: Обложка для сторис/рилс — weight: 0.8
- `ecomm_product_showcase`: E-commerce продуктовая съемка — weight: 0.75
- `footwear_boots_focus_ad`: Кампания с акцентом на обувь — weight: 0.85
- `magazine_cover_candidate`: Кандидат на обложку журнала — weight: 0.7
- `double_page_spread`: Двухстраничный разворот — weight: 0.65
- `brand_signature_visual`: Фирменный визуал бренда — weight: 0.6
- `dynamic_walk_sequence`: Динамичный кадр с движением — weight: 0.65

**Теги:** `ad`, `campaign`, `editorial`, `magazine`, `social`, `ecomm`, `product-first`, `hero`, `copy-space`

### 3. Art Direction (Арт-дирекшн)

**Назначение:** Стиль и настроение съемки.

**Вариации (13 элементов):**
- `playboy_studio_glam`: Playboy-inspired студийный гламур — weight: 1.0
- `luxury_boudoir_warm`: Теплый будуар в роскошном отеле — weight: 1.0
- `neon_night_cinematic`: Кинематографичная ночная сцена с неоном — weight: 1.0
- `backstage_mirror_bulbs`: Классическая бэкстейдж атмосфера — weight: 1.0
- `industrial_loft_edgy`: Современный индустриальный лофт — weight: 0.9
- `vintage_pinup_glam`: Ретро pin-up стиль — weight: 1.0
- `daylight_penthouse_air`: Дневной пентхаус, воздушный — weight: 0.9
- `noir_blackwhite_editorial`: Noir черно-белый редакционный — weight: 0.8
- `old_money_luxury_suite`: Old-money роскошный номер — weight: 0.85
- `minimal_high_fashion_mono`: Минимальный high-fashion монохром — weight: 0.85
- `chrome_cold_luxury`: Хром холодная роскошь — weight: 0.8
- `poolside_golden_hour`: У бассейна золотой час — weight: 0.75
- `cabaret_backstage_smoky`: Кабаре бэкстейдж дымный — weight: 0.75

**Теги:** `studio`, `high-key`, `boudoir`, `warm`, `neon`, `night`, `cinematic`, `low-key`, `backstage`, `industrial`, `vintage`, `pin-up`

### 4. Subject (Архетип модели)

**Назначение:** Типаж и характер модели.

**Вариации (10 элементов, все с "locked" для стабилизации цвета волос):**
- `blonde_bombshell`: Платиновая блондинка, длинные волны, уверенный взгляд — weight: 1.0
- `blonde_playmate_hot`: Платиновая блондинка, игривая улыбка (hot) — weight: 0.85
- `blonde_ice_queen`: Платиновая блондинка, гладкие прямые волосы, холодный взгляд — weight: 0.65
- `brunette_femme_fatale`: Глубокая брюнетка, длинные волны, интенсивный взгляд — weight: 1.0
- `brunette_midnight_siren`: Глубокая брюнетка, соблазнительный взгляд (hot) — weight: 0.9
- `brunette_slicked_power`: Глубокая брюнетка, мокрый slicked-back, доминирующий взгляд (hot) — weight: 0.6
- `dirty_blonde_sleek`: Грязная блондинка, гладкие прямые волосы, дразнящий взгляд — weight: 0.75
- `dirty_blonde_girl_next_door_hot`: Грязная блондинка, мягкий блоуаут, игривая улыбка (hot) — weight: 0.55
- `redhead_sophisticated`: Рыжая, длинные волны, элегантный взгляд — weight: 0.7
- `redhead_pinup_tease`: Рыжая, винтажные волны, игривый взгляд (sexy) — weight: 0.45

**Особенности:**
- Всегда начинается с "adult woman (25+)"
- Автоматически добавляется Hair Lock для стабилизации цвета волос
- Теги определяют совместимость с Beauty модулем

**Теги:** `blonde`, `brunette`, `redhead`, `bombshell`, `femme-fatale`, `sleek`, `sophisticated`, `confident`, `teasing`, `hair_blonde`, `hair_brunette`, `hair_red`, `hair_dirty_blonde`

### 5. Beauty (Прическа и макияж)

**Назначение:** Детали прически, макияжа и общего образа.

**Вариации (14 элементов):**

**Базовый гламур (5):**
- `glossy_waves_smoky`: Глянцевые длинные волны, дымчатые глаза — weight: 1.0
- `soft_blowout_playful`: Мягкий блоуаут, игривая улыбка — weight: 0.95
- `sleek_straight_winged`: Гладкие прямые волосы, стрелки — weight: 0.95
- `ponytail_smoky`: Конский хвост, дымчатые глаза — weight: 0.9
- `soft_waves_glowing`: Мягкие волны, сияющая кожа — weight: 0.9

**Hot/Night-glam (5):**
- `night_glam_deep_smoky`: Ночной гламур, глубокие дымчатые глаза (hot) — weight: 0.85
- `cat_eye_red_lip`: Стрелки + красные губы (hot) — weight: 0.7
- `glitter_lids_gloss`: Блестящие веки + глянец (hot) — weight: 0.65
- `metallic_smoky_highlight`: Металлические дымчатые глаза + хайлайтер — weight: 0.55
- `bronzed_sunkissed_glow`: Бронзовый загорелый сияние — weight: 0.55

**Стильные образы (4):**
- `wet_look_slickback_hot`: Мокрый slickback (edgy hot) — weight: 0.5
- `high_ponytail_cat_eye`: Высокий хвост + стрелки — weight: 0.5
- `messy_bedhair_smoky`: Растрепанные волны + дымчатые глаза (hot) — weight: 0.55
- `vintage_pinup_redlip`: Винтажный pin-up + красные губы — weight: 0.35

**Особенности:**
- Учитывается совместимость с прической из Subject модуля
- B2/B3 увеличивают вес "hot" макияжа (glitter, red lip, smoky)

**Теги:** `waves`, `blowout`, `sleek`, `straight`, `ponytail`, `smoky`, `glossy`, `playful`, `hair_waves`, `hair_blowout`, `hair_straight`, `hair_sleek`, `hair_ponytail`, `hair_slickback`, `hot`, `sexy`, `night_glam`, `glitter`, `red_lip`, `wet_look`, `makeup_smoky`, `finish_clean`

### 6. Wardrobe (Нижнее белье)

**Назначение:** Описание нижнего белья и аксессуаров.

**Вариации (19 элементов):**

**Классические базовые (5):**
- `black_lace_corset`: Черное кружевное корсетное белье — weight: 0.75
- `black_lace_bustier`: Черное кружевное бюстье — weight: 0.7
- `deep_red_satin`: Глубокий красный сатин — weight: 0.6
- `ivory_satin_balconette`: Слоновая кость, сатиновый балконет — weight: 0.55
- `ivory_lace_bodysuit`: Слоновая кость, кружевной боди — weight: 0.5

**High-impact sexy (14, для B2/B3):**
- `black_strappy_garter_set`: Черный strappy сет + подвязки (hot) — weight: 1.0
- `black_cage_bra_garter`: Черный cage-bra + подвязки (hot) — weight: 0.9
- `black_harness_inspired_set`: Harness-inspired белье (hot) — weight: 0.8
- `black_satin_triangle_thong_set`: Сатиновый треугольник + thong (hot) — weight: 0.75
- `sheer_mesh_plunge_bodysuit`: Sheer mesh plunge боди (hot) — weight: 0.85
- `black_cutout_bodysuit`: Cutout боди (hot) — weight: 0.75
- `black_teddy_open_back`: Open-back teddy (hot) — weight: 0.7
- `rhinestone_crystal_strap_set`: Crystal strap сет (statement) — weight: 0.7
- `latex_look_strappy_set`: Latex-look strappy сет (edgy hot) — weight: 0.65
- `latex_look_bodysuit_clean`: Latex-look боди (editorial hot) — weight: 0.55
- `deep_red_strappy_set`: Глубокий красный strappy сет (hot) — weight: 0.6
- `hot_pink_strappy_set`: Ярко-розовый strappy сет (statement) — weight: 0.35
- `white_lace_sheer_set`: Белый кружевной sheer сет (contrast sexy) — weight: 0.35
- `animal_print_mesh_set`: Animal print mesh сет (bold) — weight: 0.25

**Особенности:**
- Объединяется с Shoes в одну секцию "Wardrobe: ..."
- B2/B3 значительно увеличивают вес "hot" элементов (×2.2)

**Теги:** `black`, `ivory`, `red`, `lace`, `satin`, `corset`, `balconette`, `bustier`, `bodysuit`, `stockings`, `fishnet`, `hot`, `sexy`, `bold`, `strappy`, `garter`, `latex-look`, `thigh-high`, `thong`, `cutout`, `cage`, `harness`, `mesh`, `crystal`, `open-back`, `statement`, `patent`, `classic`

### 7. Shoes (Обувь/Ботинки)

**Назначение:** Описание обуви.

**Вариации (17 элементов, все с `constraints.requires: ["include floor"]`):**

**Pumps/Heels (7):**
- `black_patent_pumps`: Черные лаковые лодочки — weight: 0.8
- `black_platform_pumps`: Черные платформенные лодочки (hot) — weight: 0.9
- `black_ankle_cuff_stilettos`: Черные ankle-cuff шпильки (hot) — weight: 0.85
- `black_crystal_ankle_strap_pumps`: Crystal ankle-strap лодочки (statement) — weight: 0.55
- `beige_patent_stilettos`: Бежевые лаковые шпильки — weight: 0.45
- `red_patent_stilettos`: Красные лаковые шпильки (hot) — weight: 0.5
- `metallic_silver_pumps`: Металлические серебряные лодочки — weight: 0.25

**Strappy Sandals (2):**
- `black_laceup_stiletto_sandals`: Черные lace-up шпильки-сандалии (hot) — weight: 0.8
- `black_barely_there_sandals`: Barely-there strap сандалии (hot) — weight: 0.65

**Boots (7):**
- `knee_high_suede_boots`: Коленные замшевые сапоги — weight: 0.6
- `knee_high_leather_boots`: Коленные кожаные сапоги — weight: 0.45
- `thigh_high_patent_boots`: Замшевые сапоги до бедра (hot) — weight: 0.85
- `thigh_high_suede_boots`: Замшевые сапоги до бедра (hot) — weight: 0.65
- `thigh_high_patent_laceup_boots`: Lace-up лаковые сапоги до бедра (statement) — weight: 0.75
- `ankle_boots_stiletto`: Ankle boots шпильки — weight: 0.3
- `clear_heel_mules`: Clear-heel мюли (editorial) — weight: 0.2

**Особенности:**
- Все элементы имеют `constraints.requires: ["include floor"]`
- Объединяется с Wardrobe в секцию "Wardrobe: ..."

**Теги:** `stilettos`, `pumps`, `boots`, `patent`, `suede`, `nude`, `black`, `knee-high`, `thigh-high`, `bold`

### 8. Scene (Сцена/Локация)

**Назначение:** Описание места съемки и окружения.

**Вариации:**
- `studio_cyclorama`: Чистая студия с циклорамой
- `luxury_hotel_suite`: Роскошный номер отеля
- `neon_corridor`: Неоновый коридор
- `dressing_room_mirrors`: Гардеробная с зеркалами
- `industrial_loft`: Индустриальный лофт
- `daylight_penthouse`: Дневной пентхаус
- И другие...

**Особенности:**
- B2/B3 предпочитают ночные/backstage/hotel/noir сцены (×1.25)

**Теги:** `studio`, `hotel`, `boudoir`, `backstage`, `industrial`, `loft`, `penthouse`, `neon`, `night`, `daylight`, `time_night`, `time_golden_hour`, `set_studio`, `set_boudoir`, `set_hotel`, `set_backstage`, `set_industrial`, `set_loft`, `mood_clean`, `mood_warm`, `mood_cinematic`, `mood_edgy`, `mood_airy`, `props_mirror`, `props_bulbs`, `props_lamps`, `surface_reflective`, `surface_chrome`, `surface_glossy`, `texture_wood`, `texture_marble`, `city_bokeh`

### 9. Pose (Поза/Действие)

**Назначение:** Описание позы модели и действий.

**Вариации:**
- Различные позы: стоя, сидя, лежа, динамичные
- Взаимодействие с окружением: у кровати, у зеркала, у окна
- Жесты: руки в волосах, одна рука держит, обе руки

**Теги:** `standing`, `sitting`, `lying`, `dynamic`, `near_bed`, `near_mirror`, `near_window`, `hands_in_hair`, `one_hand_holding`, `both_hands`

### 10. Lighting (Освещение)

**Назначение:** Описание схемы освещения.

**Вариации:**
- `softbox_key_rim`: Софтбокс ключевой + контровой свет
- `warm_diffusion_rim`: Теплое рассеянное + контровой
- `cinematic_lowkey_neon`: Кинематографичный низкий ключ + неон
- `warm_practical_bulbs`: Теплые практические лампы
- `window_light_fill`: Оконный свет + заполняющий

**Теги:** `softbox`, `high-key`, `warm`, `diffusion`, `low-key`, `cinematic`, `neon`, `practical`, `window`, `daylight`, `lighting_high_key_hint`, `lighting_low_key_hint`, `lighting_window_hint`, `lighting_practical`

### 11. Camera (Камера/Композиция)

**Назначение:** Описание камеры, объектива и композиции.

**Вариации:**
- `50mm_editorial`: 50mm редакционный вид
- `35mm_low_angle`: 35mm слегка низкий угол
- `50mm_crisp_detail`: 50mm четкие детали
- `editorial_standard`: Стандартная редакционная композиция

**Теги:** `50mm`, `35mm`, `editorial`, `low-angle`, `cinematic`, `detail`, `photorealistic`, `standard`

### 12. Color (Цветокоррекция/Ретушь)

**Назначение:** Описание цветокоррекции и стиля ретуши.

**Вариации:**
- `warm_neutral_filmic`: Тепло-нейтральная кинематографичная
- `cool_chrome`: Холодная хромированная
- `premium_editorial_retouch`: Премиум редакционная ретушь
- И другие...

**Теги:** `warm`, `cool`, `neutral`, `filmic`, `chrome`, `premium`, `editorial`, `retouch`, `grading_cool`, `grading_warm`

---

## Пресеты

Пресеты задают дефолтные значения для нескольких модулей одновременно и теги для фильтрации рандомных выборов.

### Доступные пресеты (13 пресетов)

**Основные пресеты (6):**

1. **Playboy-inspired Studio Glam** — Студийная съемка, высокий ключ (cover/hero-ready)
2. **Luxury Hotel Boudoir Warm** — Теплая атмосфера будуара в роскошном отеле
3. **Neon Night Cinematic** — Кинематографичная ночная сцена с неоном
4. **Backstage Mirror Bulbs** — Классическая бэкстейдж атмосфера
5. **Industrial Loft Edgy (Latex-look)** — Современный индустриальный лофт
6. **Vintage Pin-Up Glam** — Ретро pin-up стиль

**Дополнительные пресеты (7):**

7. **Studio Clean Lookbook** — Ультра-чистый лукбук/e-comm (product clarity)
8. **Daylight Penthouse Airy** — Воздушный дневной редакционный
9. **Old-money Luxury Suite** — Классический теплый роскошный интерьер
10. **Glass Penthouse Night (City Bokeh)** — Стильный ночной пентхаус с городским боке
11. **Chrome Marble Bathroom Editorial** — Хром + мрамор роскошный сет
12. **Cabaret Backstage Smoky** — Мрачная бэкстейдж с легкой дымкой
13. **Poolside Golden Hour Glam** — Lifestyle-гламур золотого часа

**Примечание:** Полный список пресетов с их настройками (`selections` и `tags`) находится в `script.js` в константе `PRESETS_DATA`. Каждый пресет задает дефолтные выборы для модулей `goal`, `artDirection`, `scene`, `lighting`, `camera`, `color`.

**Использование пресетов:**

- Пресет можно переопределить ручными настройками модулей
- Теги пресета фильтруют рандомные выборы (soft filtering)
- Если элемент не имеет совпадающих тегов, он все равно может быть выбран (не жесткая фильтрация)

---

## Использование интерфейса

### Основные настройки (левая панель)

1. **Движок (Engine)**: выбор движка генерации (Grok/Generic)
2. **Формат (Format)**: формат изображения (IG_4x5, Story_9x16, Print_2x3)
3. **Пресет**: выбор арт-дирекшна (Playboy Studio Glam, Luxury Boudoir, и т.д.)
4. **Full-length контроль**: включение/выключение контроля полного роста
5. **Boldness**: слайдер B0-B3 с описанием каждого уровня
6. **Режим рандома**: manual / guided / full

### Модули (правая панель)

Опциональные настройки для детального контроля:
- Goal (Purpose) — цель изображения
- Art Direction — стиль съемки
- Subject archetype — типаж модели
- Beauty (Hair/Makeup) — прическа и макияж
- Wardrobe (Lingerie) — нижнее белье
- Shoes/Boots — обувь
- Scene — интерьер/локация
- Pose/Action — поза/действие
- Lighting — освещение
- Camera/Composition — камера/композиция
- Color/Retouch — цветокоррекция/ретушь

Каждый модуль можно оставить на "Автоматически" для рандомного выбора.

### Результат

После генерации отображается:

1. **Полный промпт** (на всю ширину):
   - Объединенный текст: Prompt + разделитель + Negative
   - Кнопка "Копировать всё" для копирования всего текста

2. **Две колонки** (ниже):
   - **Prompt**: только основной промпт
   - **Negative**: только негативные ограничения
   - Каждое поле со своей кнопкой копирования

3. **Экспорт Metadata**: кнопка для сохранения JSON с метаданными генерации

---

## Режимы рандома

### Manual (Ручной)
- Все модули выбираются вручную или из пресета
- Рандом не применяется
- Незаполненные модули остаются пустыми

### Guided (Частичный)
- Ручные выборы сохраняются
- Незаполненные модули заполняются рандомом
- Учитываются теги пресета (если выбран)

### Full (Полный)
- Все модули выбираются рандомом
- Учитываются теги пресета (если выбран)
- Ручные выборы игнорируются (кроме тех, что заданы пресетом)

---

## Full-length Control

### Механизм работы

При `enforceFullLength=true`:

1. **В начало промпта** добавляется:
   ```
   FULL-LENGTH LONG SHOT, head-to-toe full body in frame, 
   feet visible, shoes/boots visible, include floor, 
   camera pulled back, wide framing, 
   leave margin above head and below feet.
   ```

2. **В negative** добавляются crop-guards:
   ```
   No close-up, no portrait crop, no half-body, 
   no cropped head, no cropped feet, 
   no out-of-frame shoes/boots.
   ```

3. **Для Grok**: дополнительно дублируется в adapter для надежности

### Когда используется

- Всегда для Grok (автоматически, принудительно)
- Опционально для других движков через toggle

---

## Seedable RNG (воспроизводимость)

Генератор использует простой seedable RNG:

```javascript
class SeededRNG {
    constructor(seed) {
        this.seed = seed ?? Math.floor(Math.random() * 1000000);
    }
    next() {
        this.seed = (this.seed * 9301 + 49297) % 233280;
        return this.seed / 233280;
    }
}
```

**Использование:**

- Если передан seed, результаты воспроизводимы
- Seed сохраняется в metadata для повторной генерации
- Weighted random использует этот RNG для выбора элементов

**Воспроизведение результата:**

Используйте seed из metadata для повторной генерации с теми же параметрами:

```javascript
const generator = new PromptGenerator(lexicons, presets, seed);
const result = generator.generate(state);
```

---

## Добавление новых элементов

### Добавление варианта в словарь

Откройте `script.js` и найдите объект `LEXICONS_DATA`. Добавьте новый элемент в соответствующий массив `items`:

```javascript
LEXICONS_DATA.wardrobe = {
    items: [
        // ... существующие элементы
        {
            id: "new_item_id",
            label: "New Item Label",
            text_en: "Text for prompt",
            tags: ["tag1", "tag2"],
            weight: 1.0
        }
    ]
};
```

**Альтернативно:** Добавьте элемент в соответствующий JSON файл в `data/lexicons/`, затем встройте его в `LEXICONS_DATA`.

### Добавление нового пресета

Найдите массив `PRESETS_DATA` в `script.js` и добавьте:

```javascript
PRESETS_DATA.push({
    id: "new_preset_id",
    label: "New Preset",
    description: "Description",
    selections: {
        artDirection: "some_art_direction_id",
        scene: "some_scene_id"
    },
    tags: ["tag1", "tag2"]
});
```

**Альтернативно:** Добавьте пресет в `data/presets.json`, затем встройте его в `PRESETS_DATA`.

### Добавление нового модуля

1. Добавьте модуль в `LEXICONS_DATA`:
```javascript
LEXICONS_DATA.newModule = {
    items: [/* элементы */]
};
```

2. Добавьте обработку в генератор (`PromptGenerator.generate()`):
```javascript
// В нужном месте порядка сборки
const newModule = this.getModuleText('newModule', resolvedSelections.newModule, state.engine);
if (newModule) promptParts.push(newModule);
```

3. Добавьте в `resolveSelections()`:
```javascript
const modulesToResolve = [
    // ... существующие
    'newModule'
];
```

4. Добавьте в UI (`populateModuleSelectors()`):
```javascript
const modules = [
    // ... существующие
    { name: 'newModule', label: 'New Module', icon: '🎯' }
];
```

### Добавление нового движка (Adapter)

1. Создайте класс адаптера в `script.js`:
```javascript
class NewEngineAdapter {
    adapt(result) {
        // Логика адаптации
        return {
            ...result,
            prompt_text: modifiedPrompt,
            negative_text: modifiedNegative
        };
    }
}
```

2. Добавьте в `getAdapter()`:
```javascript
function getAdapter(engine) {
    switch (engine) {
        case 'grok':
            return new GrokAdapter();
        case 'newEngine':
            return new NewEngineAdapter();
        default:
            return new GenericAdapter();
    }
}
```

3. Добавьте опцию в `UI_CONFIG.engines`:
```javascript
engines: [
    { value: 'grok', label: 'Grok' },
    { value: 'generic', label: 'Generic' },
    { value: 'newEngine', label: 'New Engine' }
]
```

---

## Технические детали

### Архитектура

- **Чистый JavaScript (ES6+)**: без зависимостей и сборки
- **Встроенные данные**: все словари и пресеты в `script.js` (константы `LEXICONS_DATA` и `PRESETS_DATA`)
- **Модульная структура**: классы для генератора, фильтров, адаптеров
- **Seedable RNG**: для воспроизводимости результатов
- **Map-based storage**: словари и пресеты хранятся в `Map` для быстрого доступа

### Производительность

- Все данные загружаются синхронно при инициализации
- Генерация промпта происходит мгновенно (чистые вычисления)
- Нет сетевых запросов
- Weighted random оптимизирован для больших словарей

### Безопасность

- Safety фильтры применяются ко всем промптам
- Валидация на уровне продукта (не только UI)
- Автоматическая санитизация запрещенных паттернов
- Предупреждения пользователю при блокировке контента

---

## Roadmap (будущие улучшения)

- [ ] Пользовательские пресеты (сохранение в localStorage)
- [ ] Campaign packs (серии промптов)
- [ ] A/B генератор (3 варианта одной сцены)
- [ ] История генераций
- [ ] Экспорт в разные форматы
- [ ] Дополнительные движки (Midjourney, SDXL)
- [ ] Визуальный предпросмотр композиции
- [ ] Интеграция с API движков для прямой генерации

---

## Лицензия

Private project.
