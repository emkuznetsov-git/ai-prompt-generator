# Glamour Prompt Generator v4.0

Генератор промптов для AI (Nano Banana, Grok, Midjourney, SDXL) для создания glamour/lingerie изображений.

## Быстрый старт

Просто откройте `index.html` в браузере. Работает локально без сервера.

```bash
# Или через локальный сервер (опционально):
python -m http.server 8000
# Откройте http://localhost:8000/v-4-0/
```

## Что нового в v4.0

### Архитектура
- **Разделение на группы**: ENGINE → CHARACTER → LOOK → SCENE
- **Раздельное управление образом**: Outfit + Color + Material + Hosiery + Footwear
- **Расширенная цветовая палитра**: 19 цветов и паттернов
- **Больше вариативности**: 16 архетипов, 40+ нарядов, 12 видов чулок, 16 видов обуви

### Движки
- **Nano Banana** (default)
- **Grok** — с усиленным full-length framing
- **Midjourney** — формат с `--ar` и `--no`
- **SDXL / Flux**
- **Generic**

### UI
- Современный тёмный дизайн
- Color swatches для выбора палитры
- Boldness слайдер (B0-B4)
- Randomize All для быстрой генерации

## Структура настроек

### ⚙️ ENGINE
- AI Model (движок генерации)
- Format (соотношение сторон)
- Full-length toggle

### 👤 CHARACTER
- **Archetype** — типаж модели (блондинки, брюнетки, рыжие, etc.)
- **Hair** — причёска
- **Makeup** — макияж
 - **(Важно)**: в v4.0 archetype отвечает за общий образ + оттенок волос, а Hair/Makeup задаются отдельными модулями (чтобы не было конфликтов вида “2 разные причёски одновременно”).

### 👗 LOOK
- **Boldness** (B0-B4) — степень смелости
- **Outfit** — тип одежды (lingerie, bodysuit, dress, swimwear)
- **Color Palette** — цвет (visual swatches)
- **Material** — материал (lace, satin, latex, mesh)
- **Accessories** — украшения (серьги/шея)
- **Hosiery** — чулки/колготки
- **Footwear** — обувь

### 🎬 SCENE
- **Setting** — локация
- **Art Direction** — визуальный стиль
- **Pose** — поза
- **Lighting** — освещение
- **Camera** — камера/композиция
- **Color Grade** — цветокоррекция

---

## Подробная логика работы генератора

### Архитектура системы

Генератор построен на модульной архитектуре с разделением ответственности:

```
┌─────────────────────────────────────────────────────────┐
│                    UI CONTROLLER                         │
│  (управление интерфейсом, событиями, отображением)       │
└────────────────────┬──────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
┌───────▼────────┐      ┌─────────▼──────────┐
│ PROMPT         │      │ VIDEO PROMPT        │
│ GENERATOR      │      │ GENERATOR           │
└───────┬────────┘      └─────────┬──────────┘
        │                         │
        └────────────┬────────────┘
                     │
        ┌────────────▼────────────┐
        │   LEXICONS (данные)      │
        │   - archetypes           │
        │   - hair, makeup         │
        │   - outfits, materials   │
        │   - settings, poses      │
        │   - lighting, camera     │
        └──────────────────────────┘
                     │
        ┌────────────▼────────────┐
        │   SAFETY FILTER          │
        │   - фильтрация контента  │
        └──────────────────────────┘
                     │
        ┌────────────▼────────────┐
        │   ENGINE ADAPTERS        │
        │   - nano_banana          │
        │   - grok                │
        │   - midjourney          │
        │   - sdxl, generic        │
        └──────────────────────────┘
```

### 1. Инициализация (UIController.init)

При загрузке страницы происходит:

1. **Заполнение селекторов** (`populateSelectors()`):
   - Создание кастомных dropdown-меню для всех модулей (archetype, hair, makeup, outfit, etc.)
   - Группировка элементов по категориям (если есть)
   - Установка первого элемента как значения по умолчанию

2. **Настройка цветовой палитры** (`setupColorPalette()`):
   - Создание визуальных swatches для каждого цвета
   - Обработка паттернов (leopard, snake, zebra) через CSS gradients
   - Установка "black" как значения по умолчанию

3. **Настройка событий** (`setupEventListeners()`):
   - Обработчики для кнопок Generate, Randomize
   - Синхронизация boldness slider с отображением
   - Автоматическое закрытие dropdown при клике вне области

4. **Установка значений по умолчанию** (`setupDefaults()`):
   - Engine: `nano_banana`
   - Format: `story_9x16`
   - Boldness: `1` (B1 - Teasing)

### 2. Генерация промпта (PromptGenerator.generate)

Процесс сборки промпта происходит в строгом порядке:

#### Шаг 1: Framing (если включен full-length)
```javascript
if (state.enforceFullLength) {
    promptParts.push('FULL-LENGTH LONG SHOT, head-to-toe full body in frame, 
                     feet visible, shoes/boots visible, include floor, 
                     camera pulled back, wide framing, 
                     leave margin above head and below feet.');
}
```

#### Шаг 2: Art Direction
Добавляется текст из выбранного арт-направления (Playboy Glamour, Luxury Boudoir, etc.)

#### Шаг 3: Boldness Modifier
В зависимости от уровня boldness (0-3) добавляется соответствующий модификатор:
- **B0 (Elegant)**: "tasteful lingerie campaign, elegant pose, strategic coverage, classy and refined"
- **B1 (Teasing)**: "provocative glamour editorial, teasing but classy, alluring gaze, subtle seduction"
- **B2 (Bold)**: "bold glamour, implied nudity but tastefully covered, sensual framing, seductive confidence"
- **B3 (On Edge)**: "very bold glamour, on the edge, cinematic low-key, intensely seductive, still non-explicit, no sexual act"

#### Шаг 4: Subject (Персонаж)
```javascript
promptParts.push(`adult woman (25+), ${archetype.text_en}`);
```
Всегда добавляется "adult woman (25+)" для соблюдения safety правил.

#### Шаг 5: Hair Color Lock
На основе выбранного архетипа добавляется блокировка цвета волос:
```javascript
// Hair lock строится из archetype.hairShade + archetype.hairColor (для отрицаний)
const hairLock = buildHairColorLock(archetype);
// Добавляет в positive: "Hair color: platinum blonde hair (keep hair color consistent throughout)."
// Добавляет в negative: "No brunette hair, no black hair, no red hair, no dark hair."
```

#### Шаг 6-7: Hair и Makeup
Если выбрано "auto", берётся рекомендованный пресет из архетипа (defaultHair/defaultMakeup). Если выбрано не "auto", используется выбранное значение:
```javascript
if (hair && hair.id !== 'auto' && hair.text_en) {
    promptParts.push(`Hair: ${hair.text_en}`);
}
```

#### Шаг 7.5: Accessories
Если выбраны украшения (не "none"), добавляется строка:
`Accessories: ...`

#### Шаг 8: Wardrobe Assembly (assembleWardrobe)
Сборка гардероба происходит в следующем порядке:

1. **Основной наряд**: `[color] [material] [outfit]`
   - Например: "black satin bra and briefs lingerie set"

2. **Чулки** (если выбраны и не скрыты обувью):
   - Цвет чулок определяется автоматически: если основной цвет "black" или "nude", используется он, иначе "black"
   - Например: "black lace-top stockings"

3. **Обувь**:
   - Цвет обуви определяется: если основной цвет "nude" или "ivory", используется "nude", иначе "black"
   - Например: "black pointed-toe stiletto pumps"

Результат: `"Wardrobe: black satin bra and briefs lingerie set, black lace-top stockings, black pointed-toe stiletto pumps"`

#### Шаг 9-12: Scene, Pose, Lighting, Camera, Color Grade
Добавляются соответствующие описания из выбранных элементов.

#### Сборка Negative Prompt

Negative prompt всегда включает:
1. Базовые safety правила: "No explicit nudity, no sexual act, no minors."
2. Full-length guards (если включен): "No close-up, no portrait crop, no half-body, no cropped head, no cropped feet, no out-of-frame shoes/boots."
3. Технические ограничения: "No text, no watermark, no logos."
4. Артефакты: "No extra fingers/limbs, no warped hands, no distorted face, no blur, no low-res, no plastic skin."
5. Hair color lock negative (из шага 5)

### 3. Safety Filter (SafetyFilter.filter)

Перед возвратом результата промпт проходит через фильтр безопасности:

1. **Look-alike фильтр**: Удаляет упоминания реальных людей
   - Паттерн: `/\b(a-la|look like|looks like|similar to|resembling|in the style of)\s+[A-Z][a-z]+/gi`
   - Замена: "glamour archetype"

2. **Minors фильтр**: Удаляет упоминания несовершеннолетних
   - Паттерны: `teen`, `schoolgirl`, `young girl`, `underage`, `minor`, `very young`
   - Удаление: полное удаление найденных фраз

3. **Explicit фильтр**: Удаляет явный контент
   - Паттерны: `explicit`, `genital`, `sexual act`, `porn`, `xxx`
   - Удаление: полное удаление найденных фраз

4. **Нормализация**: Очистка лишних пробелов, сохранение переносов строк

Результат: объект `{ sanitized, blocked, warnings }`, где `warnings` содержит список предупреждений о внесённых изменениях.

### 4. Engine Adapters

После генерации промпт проходит через адаптер выбранного движка:

#### Nano Banana
Без изменений, возвращает результат как есть.

#### Grok
- Принудительно добавляет full-length framing в начало (если enforceFullLength)
- Усиливает negative prompt с anti-crop guards

#### Midjourney
- Конвертирует многострочный промпт в одну строку
- Добавляет `--ar [aspect_ratio]` на основе выбранного формата
- Конвертирует negative prompt в формат `--no [negatives]` (удаляет "No " и точки, заменяет на запятые)

#### SDXL / Generic
Без изменений.

### 5. Система рандомизации (SeededRNG)

Генератор использует детерминированный RNG с seed для воспроизводимости:

```javascript
class SeededRNG {
    next() {
        this.seed = (this.seed * 9301 + 49297) % 233280;
        return this.seed / 233280;
    }
    
    pick(array) {
        return array[Math.floor(this.next() * array.length)];
    }
}
```

Методы рандомизации:

1. **randomize()**: Рандомизирует все параметры (archetype, hair, makeup, outfit, color, material, hosiery, footwear, setting, artDirection, pose, lighting, camera, colorGrade)

2. **randomizeLook()**: Рандомизирует только образ (outfit, material, hosiery, footwear)

3. **randomizeScene()**: Рандомизирует только сцену (setting, artDirection, pose, lighting, camera, colorGrade)

### 6. Video Prompt Generator

Генератор видео-промптов работает на основе уже сгенерированного image prompt:

1. **Определение категории позы** (`getPoseCategory`):
   - Анализирует выбранную позу и определяет категорию: `standing`, `walking`, `seated`, `leaning`, `laying`

2. **Выбор движения камеры** (`selectCameraMovement`):
   - На основе setting и категории позы выбирает подходящее движение
   - Если не указано, выбирает автоматически

3. **Выбор движения модели** (`selectSubjectMotion`):
   - На основе категории позы выбирает доступные движения
   - Фильтруется по boldness level

4. **Выбор качества движения** (`selectMotionQuality`):
   - Фильтруется по boldness: некоторые качества доступны только для определённых уровней

5. **Выбор атмосферы** (`selectAtmosphere`):
   - На основе boldness и art direction

Результат: видео-промпт с описанием движения камеры, действия модели, качества движения и атмосферы.

### 7. UI Controller - обработка событий

#### Generate (handleGenerate)
1. Собирает состояние из всех UI элементов (`getState()`)
2. Принудительно включает full-length для определённых движков (grok)
3. Генерирует промпт через `PromptGenerator.generate()`
4. Применяет engine adapter
5. Отображает результат (`displayResult()`)

#### Randomize (handleRandomize)
1. Вызывает `generator.randomize()` для получения случайных значений
2. Обновляет все dropdown через `updateDropdownValue()`
3. Обновляет цветовую палитру
4. Автоматически запускает генерацию

#### Randomize Look / Scene
Аналогично, но рандомизирует только соответствующие группы параметров.

### 8. Кастомные Dropdown

Система dropdown реализована с учётом:

1. **Группировка по категориям**: Элементы с полем `category` группируются
2. **Многоколоночный layout**: Для больших списков используется grid layout (3-4 колонки)
3. **Fixed positioning**: Dropdown панель позиционируется относительно viewport для корректного отображения при скролле
4. **Автозакрытие**: При клике вне области или нажатии Escape
5. **Hot items**: Элементы с тегом "hot" получают специальную стилизацию

### 9. Metadata и Export

Каждый сгенерированный промпт включает metadata:
```javascript
{
    engine: 'nano_banana',
    format: 'story_9x16',
    boldness: 'B1',
    enforceFullLength: true,
    selections: { /* все выбранные параметры */ },
    seed: 123456,
    timestamp: '2024-01-13T12:00:00.000Z'
}
```

Metadata можно экспортировать в JSON для воспроизведения результата.

## Порядок сборки промпта (детальный)

```
1. FRAMING (if enforceFullLength)
   → "FULL-LENGTH LONG SHOT, head-to-toe full body in frame..."

2. ART DIRECTION
   → "Playboy-inspired glamour magazine editorial, polished sensuality..."

3. BOLDNESS MODIFIER
   → "provocative glamour editorial, teasing but classy..."

4. SUBJECT
   → "adult woman (25+), platinum blonde hair, long glamorous waves..."

5. HAIR COLOR LOCK
   → "Hair color: platinum blonde hair (keep hair color consistent throughout)."

6. HAIR (if not auto)
   → "Hair: long glamorous waves, voluminous body, glossy shine"

7. MAKEUP (if not auto)
   → "Makeup: smoky eyes, smudged liner, glossy nude lips..."

8. WARDROBE
   → "Wardrobe: black satin bra and briefs lingerie set, 
      black lace-top stockings, black pointed-toe stiletto pumps"

9. SCENE
   → "Scene: luxury hotel suite, plush king bed, velvet armchair..."

10. POSE
    → "Pose: standing full body, confident pose, one hand on hip..."

11. LIGHTING
    → "Lighting: diffused key light + gentle rim, soft gradual shadows"

12. CAMERA
    → "Camera: 50mm editorial look, camera pulled back for full-length..."

13. COLOR GRADE
    → "warm color temperature, filmic tones, gentle highlight rolloff"
```

## Safety

- **Автоматический фильтр**: no minors, no look-alike, no explicit
- **Hair color lock**: для сохранения консистентности персонажа
- **Full-length guards**: в negative prompt при включённом full-length
- **Age requirement**: всегда добавляется "adult woman (25+)"

## Файлы

```
v-4-0/
├── index.html       # Главная страница
├── style.css        # Стили (см. STYLES.md)
├── script.js        # Логика + данные
├── README.md        # Документация (этот файл)
├── ARCHITECTURE.md  # Архитектура
├── STYLES.md        # Описание стилей
└── strategy/
    └── STRATEGY.md  # Стратегия продукта
```

## Roadmap

- [x] Video extension (Sora, Runway, Kling) - реализовано
- [ ] Presets system
- [ ] History (localStorage)
- [ ] Campaign packs
